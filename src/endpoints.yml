action_endpoint:
  url: ${ACTION_ENDPOINT_URL}
tracker_store:
  type: ${TRACKER_STORE_TYPE}
  url: ${TRACKER_STORE_URL}
  db: ${TRACKER_STORE_DB}
lock_store:
  type: redis
  url: ${LOCK_STORE_URL}
  db: ${LOCK_STORE_DB}
cors:
  enabled: true
  allowed_origins:
    - "*"