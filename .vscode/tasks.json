{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Rasa Actions",
            "type": "shell",
            "command": "python -m rasa_sdk --actions src.actions",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}