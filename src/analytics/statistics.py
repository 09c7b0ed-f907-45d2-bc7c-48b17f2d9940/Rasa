from __future__ import annotations

from typing import List

from src.executor.types import StatisticalTestResult, TableResult


def run_basic_stats(tables: List[TableResult]) -> List[StatisticalTestResult]:
    """Placeholder: returns empty list. Extend with chi-square, t-tests, etc."""
    return []
